let listaConvidados = ['Jorge', 'Marcos', 'Jamilton', 'Ana', 'José', 'Maria'];

console.log(listaConvidados);

for (const x in listaConvidados) {
    console.log('Indice ' + x + ' = ' + listaConvidados[x]);
}